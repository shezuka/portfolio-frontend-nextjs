"use client";

export default function ContactMeButton() {
  const onClick = () => {};

  return (
    <button
      className="mt-4 px-6 py-2 bg-primary text-white rounded-full"
      onClick={onClick}
    >
      Contact Me
    </button>
  );
}
